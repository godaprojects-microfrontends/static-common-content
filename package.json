{
  "name": "static-commons",
  "version": "1.0.0",
  "description": "The project which will hold all static assets like images, react exteranlized js files (most importantly) and things like that",
  "main": "index.js",
  "scripts": {
    "build": "node buildData.js",
    "start": "serve -l 5030 --cors static",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Goda V",
  "license": "ISC",
  "devDependencies": {
    "prettier": "^2.0.5",
    "serve": "^11.3.2"
  },
  "prettier": {
    "singleQuote": true,
    "trailingComma": "all"
  }
}
